import React from 'react'
import dollar from './svg/dollar.svg';

const Fork = ({height = 24, width = 24, style}) => (
  <img src={dollar} height={height} width={width} style={style} alt="Dele" />
);

export default Fork;
